{
  "rules": {
    "presence": {
      "$uid": {
        ".write": "auth != null && auth.uid === $uid",
        ".read":  "auth != null"
      }
    },
    "typing": {
      "$convId": {
        "$uid": {
          ".write": "auth != null && auth.uid === $uid",
          ".read":  "auth != null"
        }
      }
    }
  }
}